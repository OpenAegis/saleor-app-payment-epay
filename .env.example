# Saleor App 配置
APP_URL=http://localhost:3000

# 回调地址配置（可选）
# 用于易支付回调，可以设置为反向代理地址以保护真实服务器地址
# 如果不设置，将使用 APP_URL
# 示例: APP_CALLBACK_URL=https://your-proxy-domain.com
APP_CALLBACK_URL=

# Turso 数据库配置（多站点共享数据）
# 获取方式：https://turso.tech/app
TURSO_DATABASE_URL=libsql://your-db-name-your-username.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# 插件管理员独立认证配置
# 这是插件专用的管理员账号，独立于Saleor用户系统
# 用于管理支付渠道和通道的增删改操作
PLUGIN_ADMIN_USERNAME=plugin_admin
PLUGIN_ADMIN_PASSWORD=your-admin-password

# 会话密钥（用于加密session，请使用随机字符串）
# 生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
PLUGIN_SESSION_SECRET=your-session-secret-key

#"fatal" | "error" | "warn" | "info" | "debug" | "trace"
APP_DEBUG=info
SECRET_KEY=your-secret-key